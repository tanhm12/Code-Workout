package main

import "fmt"

func main() {
	var h, w int
	fmt.Scanf("%d %d", &h, &w)
	// r := make([]int, h+1)
	// c := make([]int, w+1)

	// for i := 0; i < h; i++ {
	// 	fmt.Scanf("%d", &r[i])
	// }
	// for i := 0; i < w; i++ {
	// 	fmt.Scanf("%d", &c[i])
	// }

}
